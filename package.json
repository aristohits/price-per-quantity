{
  "name": "price-per-quantity",
  "version": "1.0.0",
  "description": "Price per quantity comparison app",
  "main": "index.html",
  "scripts": {
    "test": "vitest run"
  },
  "devDependencies": {
    "jsdom": "^24.1.0",
    "vitest": "^1.6.0"
  }
}
